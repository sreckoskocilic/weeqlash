services:
  server:
    build: ./server
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3001"
    environment:
      - PORT=3000
      - NODE_ENV=production
    volumes:
      - ./questions.enc:/app/questions.enc:ro
